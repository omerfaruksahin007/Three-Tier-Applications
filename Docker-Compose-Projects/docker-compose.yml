version: '3'
services:
  frontend:
    image: 889597770516.dkr.ecr.us-east-1.amazonaws.com/three-tier-frontend:latest
    build: 
      context: frontend/
    image: fronted-image
    ports: 
      - "80:80"
    depends_on:
      - backend
    networks:
      - three-tier-net 
  backend:
    image: 889597770516.dkr.ecr.us-east-1.amazonaws.com/three-tier-backend:latest
    build: 
      context: backend/
    image: backend-image
    ports: 
      - "3500:3500"
    environment:
      - MYSQL_HOST=db
      - MYSQL_USER=root
      - MYSQL_PASSWORD=mysql123
      - MYSQL_DATABASE=school
    depends_on:
      - db
    networks:
      - three-tier-net
  db:
    container_name: mysql-container
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=mysql123
      - MYSQL_DATABASE=school
    volumes:
      - mysql-data:/var/lib/mysql
volumes:
  mysql-data:
networks:
  three-tier-network:
    driver: bridge
